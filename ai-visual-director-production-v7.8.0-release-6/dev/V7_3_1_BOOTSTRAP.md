# V7.3.1 Cryptographic Bootstrap and Load Receipt

V7.3.1 adds fail-closed preflight selection, SHA-256 verification, ordered context compilation, unpredictable challenge-response linkage, a detached receipt checksum, trigger coverage checks, and validator enforcement. It proves that the expected bytes were selected and that the response had access to the session challenge. It does not make an unverifiable claim about private cognitive attention.
