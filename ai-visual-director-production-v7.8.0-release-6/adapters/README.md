# Host Adapters

Adapters translate the portable core into host-specific file-reading, vision, attachment, or validation behavior. They cannot change production acceptance criteria. If an adapter conflicts with `SKILL.md` or `rules/RUNTIME.md`, the portable core wins.
