# Core Controller — V7.8.0

1. Classify delivery mode, production mode(s), audience, factual/rights risk, operator level, requested artifacts, and evidence ceiling.
2. Create the Routing Contract. Load only selected authority files.
3. Compile a Project Contract containing stable IDs, owners, dependencies, acceptance, failure, repair, fallback, and package paths.
4. Assign `NANO_BANANA_PRO` to still prompts and `GEMINI_OMNI_FLASH` to video and conversational video-edit prompts whenever those artifacts are required.
5. Build references before dependent motion. State each reference role: starting image, identity reference, object reference, environment reference, style reference, or edit source.
6. Run timing, factual, rights, safety, accessibility, and feasibility gates before downstream artifacts.
7. Keep one principal generated beat per shot by default. Multiple beats require explicit timing and burden approval.
8. Every prompt includes acceptance and one-variable repair. Motion prompts include task, duration assumption, subject action, camera, environment, audio, continuity, and end condition.
9. Use platform-neutral handoffs for assembly and finishing. Never depend on a named consumer application.
10. Extract actual output and validate it. Failed gates block dependents; conflicting statuses fail closed.
11. Report only observed maturity. Media remains `NOT_RUN` until external execution and inspection records exist.
